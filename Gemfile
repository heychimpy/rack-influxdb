# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# Gemfile
group :development, :test do
  gem 'rubocop', '~> 1.0'
  gem 'reek'
  gem 'dry-core', '~> 1.0'  # Ensuring compatibility with Ruby 2.7, 3.0, and 3.2
end
